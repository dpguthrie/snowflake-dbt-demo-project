version: 2

models:
  - name: materialization_incremental
    description: demo to show incremental model
    columns:
      - name: customer_key
        tests:
          - unique
          - not_null

  - name: use_variables
    description: demo to show variables
    columns:
      - name: order_item_key
        tests:
          - unique
          - not_null 

  - name: get_area_of_circle
    description: demo to show udfs

  - name: my_first_model
    description: My first dbt model!
    columns:
      - name: id
        descriptions: primary key of my first dbt model
        tests:
          - unique
          - not_null
      - name: amount
        description: amount column
    tests:
      - my_first_model_assert_positive_amount

  - name: my_second_model
    description: My second dbt model!
        