version: 2

models:
  - name: orders_aggregation
    tests:
      - dbt_utils.equal_rowcount:
          compare_model:  ref('mock_target__fct_order_items')
      - dbt_utils.equality:
          compare_model: ref('mock_target__fct_order_items')
          compare_columns:
            - part_key
            - quantity
